# Sample Blog Post

This is a sample blog post! Here's some super cool (😎) markdown stuff

1. **bold**
2. _italic_
3. **_both_**

## Code Block

```rs
#[tokio::main]
async fn main() -> anyhow::Result<()> {
    email::send(EmailTarget {
        to: "gavindhondt@proton.me".into(),
        message: "Cool site! :D".into()
    }).await?;

    return Ok(());
}
```
